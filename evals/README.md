# gatui Agent Evals

This directory records lightweight Codex workflow checks.

Initial tracked scenarios:

- Port one upstream Ratatui pure-core test file into Go.
- Add missing public API behavior test-first.
- Run `.githooks/pre-commit` and report exact verification evidence.

Record future eval results as small Markdown or JSON files with:

- task name
- source files touched
- verification commands
- pass/fail outcome
- follow-up fixes needed
